// Default Linux settings for sublime-text-virtualenv:
{
    // this python executable is used in order to ...
    "executable": "python3",

    // Python LSP plugin {"LSP-pyright","LSP-basedpyright","None"}
    "LSP_plugin": "LSP-basedpyright",

    // Log level {"DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"}
    "LOG_level": "INFO",

    // Directory in which the virtualenvs are stored
    // Default location is the user's home directory
    "environment_directories": [
    	"~/.virtualenvs",
        "~/.pyenv"
    ]
}
